%ul.nav.nav-pills.nav-stacked
  - navigation_list.each do |item|
    - active_class = ''
    - active_class = 'active' if item.is_active
    %li{:class => active_class, :id => "#{item.item}"}
      %a{ :href =>  item.link}
        %i{ :class => item.icon }
          = item.item
